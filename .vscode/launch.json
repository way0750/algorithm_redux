{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Launch localhost",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:9222/${file}",
            "webRoot": "${workspaceFolder}/wwwroot",
            "sourceMaps": true
        }
    ]
}